# Copy this file to .env for local development.
# DO NOT commit .env (it is gitignored).

# Google Cloud Vision API key used by the Netlify Function (cloud OCR)
GOOGLE_VISION_API_KEY=

# Optional: force showing the dev-only OCR fixture tools UI.
# Canonical value: set to 1 to enable.
VITE_SHOW_OCR_DEV_TOOLS=1
